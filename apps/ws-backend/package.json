{
  "name": "ws-backend",
  "version": "1.0.0",
  "main": "index.js",

  "scripts": {
    "build": "rm -rf dist && tsc && cp -r src/constants dist/",
    "start": "node dist/index.js",
    "render-build": "pnpm install && pnpm run build"
  },

  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@repo/backend-common": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@repo/db": "workspace:*"
  },
  "dependencies": {
    "@types/dotenv": "^8.2.3",
    "@types/jsonwebtoken": "^9.0.9",
    "@types/ws": "^8.18.1",
    "dotenv": "^16.5.0",
    "jsonwebtoken": "^9.0.2",
    "ws": "^8.18.1"
  }
}
